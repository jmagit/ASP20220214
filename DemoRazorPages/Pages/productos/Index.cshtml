@page
@model DemoRazorPages.Pages.productos.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Culture[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Culture[0].ModifiedDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Culture) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ModifiedDate)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CultureId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CultureId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CultureId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
